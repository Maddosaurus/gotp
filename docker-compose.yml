version: '3.8'


services:
  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    image: gotp-server
    ports:
      - "50051:50051"

  rest-api:
    build:
        context: .
        dockerfile: rest/Dockerfile
    image: gotp-rest
    ports:
      - "8081:8081"
